"use client"; import { useAuth, useCart } from "@/app/providers"; import { MOCK_SHOPS, Shop } from "@/lib/mockData"; import { createOrder, firebaseInitialized } from "@/lib/firebase"; import { ArrowLeft, CreditCard, Banknote, Smartphone } from "lucide-react"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { motion } from "framer-motion"; import { Plus, Minus } from "lucide-react"; export default function CheckoutPage() { const { items, updateQuantity, totalAmount, clearCart } = useCart(); const { user } = useAuth(); const router = useRouter(); const [processing, setProcessing] = useState(false); const [checkoutError, setCheckoutError] = useState(""); const shopsInOrder = Array.from(new Set(items.map((item) => item.shopId))) .map((shopId) => MOCK_SHOPS.find((shop) => shop.id === shopId)) .filter((shop): shop is Shop => Boolean(shop)); const tax = totalAmount * 0.05; const finalAmount = totalAmount + tax; const handlePayment = async () => { if (items.length === 0) { alert("Your cart is empty. Please add items before checkout."); return; } setCheckoutError(""); setProcessing(true); try { const orderData = { userId: user?.id || "guest", items, shops: shopsInOrder.map((shop) => shop.name), totalAmount, tax, finalAmount, status: "pending", }; const orderId = firebaseInitialized ? await createOrder(orderData) : `mock-order-${Math.floor(Math.random() * 1000000)}`; clearCart(); router.push(`/order/${orderId}`); } catch (error) { console.error("Checkout failed:", error); setCheckoutError("Unable to place the order right now. Please try again."); } finally { setProcessing(false); } }; if (items.length === 0) { return ( <div className="flex flex-col items-center justify-center min-h-screen p-6 bg-gradient-to-br from-[#fafafa] via-[#f8f8f8] to-[#f5f5f7]"> <div className="w-60 h-60 bg-white/80 backdrop-blur-xl rounded-full flex items-center justify-center mb-6 shadow-lg"> <img src="https://cdn-icons-png.flaticon.com/512/11329/11329060.png" alt="Empty Cart" className="w-32 opacity-50" /> </div> <h2 className="text-xl font-bold text-gray-800">Your Cart is Empty</h2> <p className="text-gray-400 text-center mt-2 text-sm">Add items from shops before checkout.</p> </div> ); } return ( <div className="bg-gradient-to-br from-[#fafafa] via-[#f8f8f8] to-[#f5f5f7] min-h-screen pb-32"> <header className="glass-strong p-4 shadow-sm flex items-center gap-4 sticky top-0 z-10"> <button onClick={() => router.back()}> <ArrowLeft className="w-6 h-6 text-gray-700" /> </button> <h1 className="text-lg font-bold text-gray-800">Order Review</h1> </header> <div className="p-4 space-y-4"> {shopsInOrder.length > 0 && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="glass-strong p-4 rounded-3xl shadow-lg"> <p className="text-sm font-semibold text-gray-800">Ordering from</p> <div className="mt-2 flex flex-wrap gap-2"> {shopsInOrder.map((shop) => ( <span key={shop.id} className="text-xs bg-gradient-to-r from-red-50 to-pink-50 text-gray-700 px-3 py-1.5 rounded-full border border-red-100 font-medium"> {shop.name} </span> ))} </div> </motion.div> )} {checkoutError && ( <motion.div initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} className="bg-red-50/80 backdrop-blur-xl text-red-700 border border-red-200/50 p-4 rounded-3xl shadow-md"> {checkoutError} </motion.div> )} <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.1 }} className="glass-strong p-4 rounded-3xl shadow-lg"> <h3 className="font-bold text-gray-800 mb-4">Items</h3> <div className="space-y-4"> {items.map((item) => ( <motion.div key={item.id} initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} className="flex justify-between items-center py-3 border-b border-gray-100/50 last:border-0" > <div className="flex items-start gap-2 flex-1"> <div className="w-4 h-4 mt-1 border border-green-600 flex items-center justify-center rounded-[2px]"> <div className="w-2 h-2 bg-green-600 rounded-full"></div> </div> <div> <h3 className="text-sm font-bold text-gray-800">{item.name}</h3> <p className="text-xs text-gray-500">₹{item.price}</p> </div> </div> <div className="flex items-center gap-2"> <motion.div className="flex items-center gap-2 bg-green-50/80 backdrop-blur-sm border border-green-100 px-2 py-1 rounded-lg" key={`${item.id}-${item.quantity}`} > <motion.button whileTap={{ scale: 0.95 }} onClick={() => updateQuantity(item.id, item.quantity - 1)} className="text-green-700 font-bold px-2 py-1 hover:bg-green-100 rounded transition-colors" > <Minus className="w-4 h-4" /> </motion.button> <motion.span key={item.quantity} initial={{ scale: 1.2 }} animate={{ scale: 1 }} className="text-sm font-bold text-green-700 w-6 text-center" > {item.quantity} </motion.span> <motion.button whileTap={{ scale: 0.95 }} onClick={() => updateQuantity(item.id, item.quantity + 1)} className="text-green-700 font-bold px-2 py-1 hover:bg-green-100 rounded transition-colors" > <Plus className="w-4 h-4" /> </motion.button> </motion.div> <motion.div key={`price-${item.id}-${item.quantity}`} initial={{ opacity: 0.5 }} animate={{ opacity: 1 }} className="text-sm font-medium text-gray-700 w-16 text-right" > ₹{item.price * item.quantity} </motion.div> </div> </motion.div> ))} </div> </motion.div> <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: 0.2 }} className="relative bg-gradient-to-br from-amber-50 via-orange-50 to-red-50 backdrop-blur-xl p-6 rounded-3xl shadow-2xl border border-amber-200/50 text-center mb-6 ring-1 ring-amber-100/50 overflow-hidden" > <div className="absolute top-0 right-0 w-32 h-32 bg-gradient-to-br from-amber-200/20 to-orange-200/20 rounded-full blur-3xl"></div> <div className="absolute bottom-0 left-0 w-24 h-24 bg-gradient-to-tr from-red-200/20 to-pink-200/20 rounded-full blur-2xl"></div> <div className="relative z-10"> <motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.3 }} className="text-gray-600 text-xs font-bold uppercase tracking-widest mb-2" > Total Amount to Pay </motion.p> <motion.h2 key={finalAmount} initial={{ scale: 0.8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ type: "spring", stiffness: 200, delay: 0.4 }} className="text-5xl font-black bg-gradient-to-r from-red-600 via-orange-600 to-red-600 bg-clip-text text-transparent drop-shadow-sm" > ₹{finalAmount.toFixed(0)} </motion.h2> <motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.5 }} className="text-xs text-gray-500 mt-2" > Includes all taxes & charges </motion.p> </div> </motion.div> <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.3 }} className="glass-strong p-4 rounded-3xl shadow-lg"> <h3 className="text-sm font-bold text-gray-800 mb-4">Bill Details</h3> <div className="space-y-3"> <div className="flex justify-between text-sm text-gray-600"> <span>Item Total</span> <motion.span key={totalAmount} initial={{ opacity: 0.5 }} animate={{ opacity: 1 }} className="font-medium" > ₹{totalAmount.toFixed(2)} </motion.span> </div> <div className="flex justify-between text-sm text-gray-600"> <span>Taxes & Charges</span> <motion.span key={tax} initial={{ opacity: 0.5 }} animate={{ opacity: 1 }} className="font-medium" > ₹{tax.toFixed(2)} </motion.span> </div> <div className="flex justify-between text-sm text-gray-600 bg-green-50/80 backdrop-blur-sm p-2.5 rounded-xl -mx-2 border border-green-100"> <span className="font-medium">Platform Fee</span> <span className="text-green-600 font-bold">FREE</span> </div> <div className="border-t-2 border-gray-200 my-2 pt-3 flex justify-between text-base font-bold text-gray-900 bg-gradient-to-r from-gray-50 to-gray-100/50 p-3 rounded-xl -mx-3"> <span>To Pay</span> <motion.span key={finalAmount} initial={{ scale: 0.9 }} animate={{ scale: 1 }} className="text-red-600" > ₹{finalAmount.toFixed(2)} </motion.span> </div> </div> </motion.div> <h3 className="text-xs font-bold text-gray-500 uppercase ml-1 mt-6">Payment Methods</h3> <div className="space-y-3 pb-20"> <PaymentOption icon={<Smartphone className="text-blue-600" />} title="UPI" sub="Google Pay, PhonePe, Paytm" onClick={handlePayment} /> <PaymentOption icon={<CreditCard className="text-red-600" />} title="Cards" sub="Visa, Mastercard, RuPay" onClick={handlePayment} /> <PaymentOption icon={<Banknote className="text-green-600" />} title="Pay on Pickup" sub="Cash or QR at Shop" onClick={handlePayment} /> </div> </div> {processing && ( <div className="fixed inset-0 z-50 glass-strong flex flex-col items-center justify-center"> <motion.div animate={{ rotate: 360 }} transition={{ repeat: Infinity, duration: 1, ease: "linear" }} className="w-16 h-16 border-4 border-primary border-t-transparent rounded-full mb-4" /> <h3 className="text-xl font-bold text-gray-800">Processing Payment...</h3> <p className="text-gray-500 text-sm">Do not press back or close</p> </div> )} </div> ); } function PaymentOption({ icon, title, sub, onClick }: { icon: any; title: string; sub: string; onClick: () => void }) { return ( <motion.button whileTap={{ scale: 0.98 }} onClick={onClick} className="w-full glass-strong p-4 rounded-2xl shadow-sm border border-gray-100/50 flex items-center gap-4 hover:border-primary/50 hover:bg-white/50 transition-all text-left group" > <div className="w-12 h-12 bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl flex items-center justify-center group-hover:shadow-md transition-all"> {icon} </div> <div className="flex-1"> <h4 className="font-bold text-gray-800">{title}</h4> <p className="text-xs text-gray-500">{sub}</p> </div> <div className="w-5 h-5 rounded-full border-2 border-gray-300 group-hover:border-primary group-hover:bg-primary/10 transition-all"></div> </motion.button> ); }